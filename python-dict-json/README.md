# Python Dictionary App

A simple and robust command-line dictionary application built with Python. Users can search for word translations, and if a word is not found, they can add a new definition to the dictionary. All data is automatically saved and loaded using a `dictionary.json` file.

## How to Run

Make sure you have Python installed. Navigate to the folder in your terminal and run the following command:

```bash
python main.py
```
